# 照片管理器 - 使用说明

## 功能特性

### 1. 文件格式支持
- **JPG格式**: 完整支持预览、分类、管理
- **RAW格式**: 支持20+种主流RAW格式（CR2/CR3/NEF/ARW/DNG/RAF等），无需预览即可分类管理

### 2. 界面布局

#### 左侧面板 - 目录树
- 显示所有添加的文件夹目录结构
- 右键文件夹可设置"返图/未返图"状态标记
- 状态标记会同步显示在JPG和RAW的对应文件夹

#### 中间面板
- **上部**: JPG图片缩略图预览区，支持多选
- **下部**: 操作日志，实时显示系统操作结果

#### 右侧面板
- **上部**: 大图预览区，支持小/中/大三种尺寸切换
- **下部**: 文件详细属性（大小、创建时间、EXIF信息等）

### 3. 核心功能

#### 一键分类
1. 选择需要分类的文件夹
2. 点击工具栏"一键分类"按钮
3. 系统自动创建三个子文件夹：
   - `jpg` - 存放所有JPG格式图片
   - `raw` - 存放所有RAW格式文件
   - `其他` - 存放其他格式文件

#### 新建分类
1. 在缩略图区选中需要分类的JPG图片（可多选）
2. 点击工具栏"新建分类"按钮
3. 输入文件夹名称（留空则自动使用"日期+时间"命名）
4. 确认后选中文件将移动到新文件夹

#### 一键同步分类
1. 确保已完成"一键分类"，存在`jpg`和`raw`文件夹
2. 在`jpg`文件夹内完成图片分类后
3. 选择父文件夹，点击"一键同步分类"
4. 系统自动将RAW文件按照JPG的分类结构同步移动

#### 返图状态标记
1. 在左侧目录树中右键点击文件夹
2. 选择"标记为已返图"或"标记为未返图"
3. 标记文字显示在文件夹名称后方
4. JPG和RAW对应文件夹状态自动同步

### 4. 交互操作
- **双击图片**: 调用系统默认图像查看器打开
- **拖动面板边界**: 调整各面板大小
- **Ctrl+点击**: 多选图片
- **Shift+点击**: 连续多选

## 打包EXE

### 方法一：使用打包脚本
1. 双击运行 `build_exe.bat`
2. 等待自动安装依赖并打包
3. 生成的EXE文件位于 `dist/照片管理器.exe`

### 方法二：手动命令
```bash
pip install -r requirements.txt
pyinstaller --noconfirm --clean --windowed --name 照片管理器 --onefile photo_manager.py
```

## 运行程序

### 开发模式运行
```bash
pip install -r requirements.txt
python photo_manager.py
```

### 直接运行EXE
双击 `dist/照片管理器.exe` 即可使用，无需安装其他依赖

## 使用流程建议

1. **导入文件夹**: 点击"添加文件夹"选择照片所在目录
2. **一键分类**: 先将文件按格式分类到jpg/raw/其他
3. **JPG分类**: 在jpg文件夹内新建分类，整理照片
4. **同步RAW**: 点击"一键同步分类"，RAW自动跟随JPG分类
5. **状态标记**: 完成返图后右键标记状态

## 注意事项

- 分类操作会实际移动文件，请确保文件已备份
- RAW文件不提供预览功能，但会跟随JPG进行同步分类
- 建议先在测试文件夹中试用熟悉操作
